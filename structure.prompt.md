You are a senior Java Spring Boot architect.

Your task is to generate the COMPLETE initial project skeleton for my microservices project.

DO NOT explain anything.
DO NOT skip files.
DO NOT leave TODOs unless absolutely necessary.

Generate the entire folder structure with production-quality boilerplate.

==========================================================
PROJECT INFORMATION
==========================================================

Project Name:
Smart Courier

Architecture:
Maven Multi Module Project

Java:
21

Spring Boot:
4.0.7

Packaging:
Jar

Configuration Format:
application.yml

Root Package:
com.smartcourier

==========================================================
PROJECT STRUCTURE
==========================================================

Create this exact structure.

smart-courier/

├── pom.xml
│
├── discovery-server/
│ ├── pom.xml
│ └── src/main/
│
├── api-gateway/
│ ├── pom.xml
│ └── src/main/
│
├── user-service/
│ ├── pom.xml
│ └── src/main/
│
├── booking-service/
│ ├── pom.xml
│ └── src/main/
│
├── tracking-service/
│ ├── pom.xml
│ └── src/main/
│
└── notification-service/
├── pom.xml
└── src/main/

==========================================================
VERSION COMPATIBILITY (CRITICAL)
==========================================================

Spring Boot, Spring Cloud, and Spring AI versions MUST be
compatible with each other. Using mismatched versions will
cause ClassNotFoundException and build failures.

For Spring Boot 4.0.x, use:

- Spring Cloud: 2025.1.x (Oakwood release train)
  DO NOT use 2025.0.x — that is for Spring Boot 3.5.x only.

- Spring AI: 1.0.x
  Artifact names changed in 1.0.0 (see DEPENDENCIES section).

Always verify compatibility at:
https://spring.io/projects/spring-cloud

==========================================================
PARENT POM (VERY IMPORTANT)
==========================================================

Follow STANDARD Spring Boot Maven project conventions.

DO NOT invent your own Maven structure.

The parent pom.xml MUST inherit from:

org.springframework.boot:spring-boot-starter-parent

using

<parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.0.7</version>
    <relativePath/>
</parent>

Properties MUST include:

<properties>
    <java.version>21</java.version>
    <spring-cloud.version>2025.1.0</spring-cloud.version>
    <spring-ai.version>1.0.0</spring-ai.version>
</properties>

Packaging must be:

pom

The parent pom should remain CLEAN and MINIMAL.

It should ONLY contain:

- parent
- groupId
- artifactId
- version
- packaging
- name
- description
- modules
- properties
- dependencyManagement
    - Spring Cloud BOM only
    - Spring AI BOM (only if required)
- repositories (only if absolutely required)

DO NOT manually configure:

- maven-compiler-plugin
- maven-surefire-plugin
- spring-boot-maven-plugin versions
- compiler source
- compiler target
- encoding plugins
- pluginManagement

Spring Boot Starter Parent already manages these.

DO NOT duplicate configuration already provided by Spring Boot.

The parent pom should look like a standard Spring Boot multi-module project found in production repositories.

==========================================================
DEPENDENCY MANAGEMENT
==========================================================

Manage versions ONLY where appropriate.

Use dependencyManagement ONLY for:

- Spring Cloud BOM
- Spring AI BOM (if used)

DO NOT place normal project dependencies such as

- JJWT
- SpringDoc
- PostgreSQL
- Spring Web
- Spring Security

inside dependencyManagement.

Those belong inside the child module dependencies.

The purpose of dependencyManagement is version management only.

==========================================================
CHILD MODULE POM RULES
==========================================================

Every child module MUST inherit from the parent pom.

Every child pom.xml should begin with

<parent>
    <groupId>com.smartcourier</groupId>
    <artifactId>smart-courier</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <relativePath>../pom.xml</relativePath>
</parent>

Each child pom should contain ONLY

- parent
- artifactId
- name
- dependencies
- build
    - spring-boot-maven-plugin

DO NOT duplicate

- Java version
- Spring Boot version
- Spring Cloud version
- Maven plugin versions

The child pom.xml should be concise and follow the same style generated by Spring Initializr.

==========================================================
FOLLOW SPRING INITIALIZR STYLE
==========================================================

Generate pom.xml files that look exactly like those generated by Spring Initializr.

Do NOT generate a custom Maven layout.

If there is a choice between

1. custom Maven configuration

or

2. Spring Initializr convention

always choose Spring Initializr convention.

The generated project should feel like it was originally created from Spring Initializr and then converted into a Maven multi-module project.

==========================================================
MODULES
==========================================================

Create these modules.

discovery-server

api-gateway

user-service

booking-service

tracking-service

notification-service

==========================================================
DEPENDENCIES
==========================================================

Discovery Server

- spring-cloud-starter-netflix-eureka-server

---

API Gateway

IMPORTANT: In Spring Cloud 2025.1.x (Gateway 5.0), the old
"spring-cloud-starter-gateway" artifact was REMOVED.
You MUST use the specific web stack variant:

- spring-cloud-starter-gateway-server-webflux (for reactive/WebFlux gateway)
- Spring Security
- OAuth2 Resource Server
- Eureka Discovery Client

DO NOT use "spring-cloud-starter-gateway" — it does not exist
in the 2025.1.x BOM and will cause a missing version error.

---

User Service

- Spring Web
- Spring Data JPA
- PostgreSQL Driver
- Validation
- Lombok
- Spring Security
- OAuth2 Resource Server
- Eureka Discovery Client

---

Booking Service

- Spring Web
- Spring Data JPA
- PostgreSQL Driver
- Validation
- Lombok
- Spring Security
- OAuth2 Resource Server
- OpenFeign
- Eureka Discovery Client

---

Tracking Service

- Spring Web
- Spring Data JPA
- PostgreSQL Driver
- Validation
- Lombok
- Spring Security
- OAuth2 Resource Server
- OpenFeign
- Eureka Discovery Client

---

Notification Service

- Spring Web
- Spring Data JPA
- PostgreSQL Driver
- Validation
- Lombok
- Spring Security
- OAuth2 Resource Server
- OpenFeign
- Eureka Discovery Client

==========================================================
MANUAL DEPENDENCIES
==========================================================

Add these manually inside pom.xml where required.

User Service

- JJWT (io.jsonwebtoken:jjwt-api, jjwt-impl, jjwt-jackson)
- SpringDoc OpenAPI (org.springdoc:springdoc-openapi-starter-webmvc-ui)

Booking Service

- JJWT (io.jsonwebtoken:jjwt-api, jjwt-impl, jjwt-jackson)
- SpringDoc OpenAPI (org.springdoc:springdoc-openapi-starter-webmvc-ui)
- Spring AI OpenAI Starter

IMPORTANT: In Spring AI 1.0.0, artifact names were RENAMED.
The old "spring-ai-openai-spring-boot-starter" does NOT exist.
You MUST use the new name:

  org.springframework.ai:spring-ai-starter-model-openai

This artifact's version is managed by the Spring AI BOM.
DO NOT add a <version> tag — the BOM handles it.

Tracking Service

- JJWT (io.jsonwebtoken:jjwt-api, jjwt-impl, jjwt-jackson)
- SpringDoc OpenAPI (org.springdoc:springdoc-openapi-starter-webmvc-ui)

Notification Service

- JJWT (io.jsonwebtoken:jjwt-api, jjwt-impl, jjwt-jackson)
- SpringDoc OpenAPI (org.springdoc:springdoc-openapi-starter-webmvc-ui)

Gateway

- JJWT (io.jsonwebtoken:jjwt-api, jjwt-impl, jjwt-jackson)
- SpringDoc OpenAPI WebFlux (org.springdoc:springdoc-openapi-starter-webflux-ui)

Use the latest stable version officially compatible with Spring Boot 4.0.7. Do not use milestone, snapshot, RC, or beta releases.

==========================================================
JAVA PACKAGE STRUCTURE
==========================================================

Create the proper Java package.

Discovery

com.smartcourier.discovery

Gateway

com.smartcourier.gateway

User

com.smartcourier.userservice

Booking

com.smartcourier.bookingservice

Tracking

com.smartcourier.trackingservice

Notification

com.smartcourier.notificationservice

==========================================================
MAIN CLASS
==========================================================

Generate the Spring Boot starter class inside every module.

Example

@SpringBootApplication
public class UserServiceApplication

Same for every module.

Use proper naming conventions.

==========================================================
RESOURCE STRUCTURE
==========================================================

Inside every module create

src/main/resources

application.yml

==========================================================
APPLICATION CONFIGURATION
==========================================================

Populate every application.yml.

Do NOT leave it empty.

Each service should have

spring:

application:

name:

server:

port:

logging:

level:

management:

endpoints:

web:

exposure:

include: health,info

---

Discovery Server

Port

8761

Configure Eureka Server properly.

---

Gateway

Port

8080

Configure

- Eureka Client
- Gateway
- Discovery Locator
- Actuator
- Placeholder routes for every service

---

User Service

Port

8081

Configure

- PostgreSQL datasource
- Hibernate
- JPA
- Eureka Client
- JWT placeholder properties
- Logging

---

Booking Service

Port

8082

Configure

- PostgreSQL datasource
- JPA
- Eureka Client
- Feign
- Spring AI placeholder configuration
- JWT placeholder

---

Tracking Service

Port

8083

Configure

- PostgreSQL datasource
- JPA
- Eureka Client
- Feign
- JWT placeholder

---

Notification Service

Port

8084

Configure

- PostgreSQL datasource
- JPA
- Eureka Client
- Feign
- JWT placeholder

==========================================================
DATABASE PLACEHOLDERS
==========================================================

Use dummy PostgreSQL configuration.

Example

Host

localhost

Port

5432

Username

dummy_user

Password

dummy_password

Database names

user_service_db

booking_service_db

tracking_service_db

notification_service_db

==========================================================
JWT PLACEHOLDER
==========================================================

Create placeholder configuration.

jwt:

secret:

expiration:

refresh-expiration:

==========================================================
SPRING AI PLACEHOLDER
==========================================================

Create placeholder values.

api-key

model

temperature

==========================================================
LOGGING
==========================================================

Configure useful logging.

==========================================================
ACTUATOR
==========================================================

Enable

health

info

==========================================================
BOILERPLATE
==========================================================

Generate a project that can be opened directly in IntelliJ IDEA.

Everything should compile.

No missing parent references.

No missing package declarations.

No placeholder Java files besides the Spring Boot main class.

No README.

No Docker.

No business logic.

No entities.

No controllers.

No services.

No repositories.

This task is ONLY for creating the complete project skeleton.

==========================================================
STRICT REQUIREMENT
==========================================================

Do NOT guess Maven structure.

Do NOT invent plugin configurations.

Do NOT manually configure anything already managed by Spring Boot Starter Parent.

Prefer the official Spring Boot project structure over verbosity.

When in doubt, generate exactly what experienced Spring Boot developers would commit in a production repository.

==========================================================
OUTPUT
==========================================================

Generate every directory and every file with full contents.

At the very end provide a section titled:

"DUMMY VALUES TO REPLACE"

List every dummy value, where it appears, and what it should eventually be replaced with, including:

- Database host
- Database username
- Database password
- JWT secret
- JWT expiration
- Spring AI API key
- Spring AI model
- Gateway route placeholders
- Any other placeholder values introduced
